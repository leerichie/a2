name: a2
description: 'A friendly, local-first food and fitness companion.'

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+56

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.5
  http: ^1.6.0
  package_info_plus: ^10.2.1
  image_picker: ^1.2.0
  url_launcher: ^6.3.1
  google_mlkit_text_recognition: ^0.15.0
  google_sign_in: ^7.2.0
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  sign_in_with_apple: ^6.1.4
  share_plus: ^13.3.0
  path_provider: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.6

flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: 'assets/branding/app_icon.png'
  min_sdk_android: 21

flutter_native_splash:
  color: '#F6F4EE'
  image: 'assets/branding/app_icon.png'
  android_12:
    color: '#F6F4EE'
    image: 'assets/branding/app_icon.png'

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/data/ashley_history.json
    - assets/branding/dev_logo.png
    - assets/branding/app_icon.png
    - assets/parser/shared/
    - assets/parser/shared/nutrition/
    - assets/parser/en/
    - assets/parser/en/lexicon/
    - assets/parser/pl/
    - assets/parser/pl/lexicon/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
